<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- For sharing crash logs -->
    <files-path
        name="crash_logs"
        path="logs/" />

    <!-- For sharing exported equalizer profiles -->
    <external-cache-path
        name="exported_profiles"
        path="exports/" />

    <!-- For sharing backup files -->
    <external-cache-path
        name="backups"
        path="backups/" />
</paths>
